source "https://rubygems.org"

# fastlane drives the browser-only TestFlight pipeline (build + upload) on the
# GitHub-hosted macOS runner. See fastlane/Fastfile and docs/TESTFLIGHT.md.
gem "fastlane"
